{
  "name": "rainholm-garden",
  "version": "1.3.0",
  "description": "A hand-painted little garden and greenhouse you can plant, water and harvest in — plus two pixel cats (yours and your AI's) wandering around.",
  "private": true,
  "type": "module",
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "start": "node start.mjs",
    "test": "node --test tests/*.test.mjs",
    "build:cloudflare": "node cloudflare/build.mjs",
    "build": "npm run build:cloudflare",
    "deploy": "wrangler deploy",
    "dev:cloudflare": "wrangler dev --local-protocol https",
    "test:cloudflare": "npm run build:cloudflare && node --test tests/cloudflare/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "express": "5.2.1"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "miniflare": "5.20260918.0-alpha",
    "wrangler": "4.135.0"
  },
  "cloudflare": {
    "bindings": {
      "GARDEN_USER_KEY": {
        "description": "Private player key: 32-256 random Base64/Base64url characters. Generate with node cloudflare/keys.mjs."
      },
      "GARDEN_AI_KEY": {
        "description": "Private AI key: generate separately from the player key. Never reuse another service token."
      }
    }
  }
}
